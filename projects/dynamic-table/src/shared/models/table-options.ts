export class TableOptions {
  inlineEdit?: boolean;
  pagination?: boolean;
  hasLoading?: boolean;
  hasFilter?: boolean; 
  singleSelect?: boolean; 
  pageSize?: number;
  pageSizeOptions?: number[];
}
