dist: trusty
sudo: false

language: node_js
node_js:
  - "8"

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

cache:
  directories:
    - ./node_modules

install:
  - npm install

jobs:
  include:
  # - stage: lint
  #   script:
  #   - ng lint
  # - stage: test
  #   script:
  #   - npm run test:lib-coverage
  #   - npm run report-coverage:lib
  - stage: build & publish showcase
    script:
    - npm run build:demo
    deploy:
      provider: pages
      skip_cleanup: true
      github_token: 8374c9ea-d2e5-454c-90c7-b2708db380bc
      local_dir: dist/dynamic-table-showcase
    if: branch = master
  # - stage: build & publish lib
  #   script:
  #   - npm run build:lib
  #   - npm run publish:lib
  #   if: branch = master