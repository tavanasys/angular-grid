dist: trusty
sudo: false

language: node_js
node_js:
  - "8"

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

cache:
  directories:
    - ./node_modules

install:
  - npm install

jobs:
  include:
  # - stage: lint
  #   script:
  #   - ng lint
  # - stage: test
  #   script:
  #   - npm run test:lib-coverage
  #   - npm run report-coverage:lib
  - stage: build & publish showcase
    script:
    - npm run build:showcase
    deploy:
      provider: pages
      skip_cleanup: true
      github_token: d3b1e804b904708d27cfb44f3b322d210aabc679
      local_dir: dist/dynamic-table-showcase
    if: branch = master
  # - stage: build & publish lib
  #   script:
  #   - npm run build:lib
  #   - npm run publish:lib
  #   if: branch = master